@model IEnumerable<HR_Application_CO_OP_HOS.Models.LoanCategory>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DefaultAllocation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LoanCategoryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsFixedLoanInstallments)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DefaultAllocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LoanCategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsFixedLoanInstallments)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
