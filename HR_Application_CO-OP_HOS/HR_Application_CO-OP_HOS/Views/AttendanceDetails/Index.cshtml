@model IEnumerable<HR_Application_CO_OP_HOS.Models.AttendanceDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RelatedPartyID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkedMonth)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkedYear)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsOTRecommended)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RelatedPartyID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkedMonth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkedYear)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsOTRecommended)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
